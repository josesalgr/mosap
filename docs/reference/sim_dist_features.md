# Simulated feature distribution

Example distribution of feature amounts across planning units.

## Usage

``` r
data(sim_dist_features)
```

## Format

A data frame linking planning units and features with an `amount`
column.
