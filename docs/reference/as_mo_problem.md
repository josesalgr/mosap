# Create a multi-objective problem wrapper

Create a multi-objective problem wrapper

## Usage

``` r
as_mo_problem(x)
```

## Arguments

- x:

  A `Data` object created by `prioriactions::inputData()` (or a
  compatible object).

## Value

An object of class `MOProblem`.
