% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_locked_pu.R
\name{add_locked_pu}
\alias{add_locked_pu}
\title{Add locked planning units to a problem}
\usage{
add_locked_pu(
  x,
  locked_in = NULL,
  locked_out = NULL,
  pu_status = NULL,
  overwrite = TRUE,
  status_overrides = FALSE
)
}
\arguments{
\item{x}{A \code{Problem} object created with \code{\link[=inputData]{inputData()}} or
\code{\link[=inputDataSpatial]{inputDataSpatial()}}.}

\item{locked_in}{Optional locked-in specification. Can be \code{NULL}, a column
name in \code{x$data$pu_data_raw}, a logical vector, or a vector of PU ids.}

\item{locked_out}{Optional locked-out specification. Can be \code{NULL}, a column
name in \code{x$data$pu_data_raw}, a logical vector, or a vector of PU ids.}

\item{pu_status}{Optional status specification using Marxan-style codes.
Can be \code{NULL}, a column name in \code{x$data$pu_data_raw}, or a vector of length
\code{nrow(x$data$pu)}. Values \code{2} mean locked-in and values \code{3} mean locked-out.}

\item{overwrite}{Logical. If \code{TRUE}, replace existing \code{locked_in} and
\code{locked_out} columns in \code{x$data$pu}. If \code{FALSE}, merge with existing values
using logical OR.}

\item{status_overrides}{Logical. If \code{TRUE}, \code{pu_status} overrides any
existing or explicitly supplied \code{locked_in} / \code{locked_out} assignments.
If \code{FALSE} (default), explicit \code{locked_in} and \code{locked_out} take precedence.}
}
\value{
The updated \linkS4class{problem} object with logical columns
\code{x$data$pu$locked_in} and \code{x$data$pu$locked_out}.
}
\description{
Define locked-in and locked-out planning units for a \code{Problem} object.

This function is intended to be used \emph{after} \code{\link[=inputData]{inputData()}}. It updates
the internal planning-unit table stored in \code{x$data$pu} by creating or
replacing the logical columns \code{locked_in} and \code{locked_out}.

Lock information can be supplied in three ways:
\enumerate{
\item As a column name in \code{x$data$pu_data_raw},
\item As a logical vector of length equal to the number of planning units,
\item As a vector of planning-unit ids.
}

In addition, \code{pu_status} can be used with Marxan-style status codes:
values \code{2} indicate locked-in planning units and values \code{3} indicate
locked-out planning units.
}
\details{
\strong{Philosophy.}
The role of \code{\link[=inputData]{inputData()}} is to construct and normalize the problem inputs.
Locking planning units is treated as a separate modeling step, so this
function allows users to define or update locked planning units explicitly
after the \code{Problem} object has been created.

\strong{How inputs are interpreted.}

For \code{locked_in} and \code{locked_out}, the function accepts:
\itemize{
\item \code{NULL}: do not modify that side;
\item a single character string: interpreted as a column name in
\code{x$data$pu_data_raw};
\item a logical vector of length \code{nrow(x$data$pu)};
\item a vector of planning-unit ids.
}

For \code{pu_status}, the function accepts:
\itemize{
\item \code{NULL}: ignored;
\item a single character string: interpreted as a column name in
\code{x$data$pu_data_raw};
\item a vector of length \code{nrow(x$data$pu)} with status values.
}

When \code{pu_status} is supplied, values \code{2} are interpreted as locked-in and
values \code{3} as locked-out. By default, explicit \code{locked_in} / \code{locked_out}
inputs take precedence over \code{pu_status}.
}
\examples{
\dontrun{
# Read lock columns from original PU data
p <- add_locked_pu(
  p,
  locked_in = "locked_in",
  locked_out = "locked_out"
)

# Read Marxan-style status column
p <- add_locked_pu(
  p,
  pu_status = "status"
)

# Lock by PU ids
p <- add_locked_pu(
  p,
  locked_in = c(1, 5, 8),
  locked_out = c(10, 12)
)

# Lock using logical vectors
p <- add_locked_pu(
  p,
  locked_in = c(TRUE, FALSE, TRUE, FALSE),
  overwrite = TRUE
)
}

}
\seealso{
\code{\link[=inputData]{inputData()}}, \code{\link[=add_actions]{add_actions()}}
}
