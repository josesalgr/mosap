% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problem.R
\name{problem}
\alias{problem}
\title{Configure mathematical model (legacy-compatible preset)}
\usage{
problem(
  x,
  model_type = c("minimizeCosts", "maximizeBenefits"),
  budget = 0,
  blm = 0,
  curve = 1,
  segments = 3
)
}
\arguments{
\item{x}{Data object (class "Data") created with inputData()/inputDataSpatial().}

\item{model_type}{character. "minimizeCosts" or "maximizeBenefits".}

\item{budget}{numeric. Stored in model_args (compat/printing).}

\item{blm}{numeric. Stored in model_args (compat/printing).}

\item{curve}{integer. Stored in model_args (compat/printing).}

\item{segments}{integer. Stored in model_args (compat/printing).}
}
\value{
Updated \code{Data} object with preset stored in \code{x$data$model_args}.
}
\description{
Legacy-compatible helper that stores a model preset inside the \code{Data} object.
In the new pipeline, the MILP is built inside \code{solve()}. This function does NOT
create a C++ model pointer; it only configures \code{x$data$model_args} for \code{.pa_build_model()}.
}
