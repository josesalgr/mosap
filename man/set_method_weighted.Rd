% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_method_weighted.R
\name{set_method_weighted}
\alias{set_method_weighted}
\title{Set multi-objective method: weighted sum}
\usage{
set_method_weighted(x, aliases, weights, normalize = FALSE)
}
\arguments{
\item{x}{A \code{prioriactions::Data} object (or an \code{"MOProblem"}).}

\item{aliases}{\code{character}. Objective aliases to combine (e.g. \code{c("cost","frag")}).}

\item{weights}{\code{numeric}. Non-negative weights, same length as \code{aliases}.}

\item{normalize}{\code{logical}. If \code{TRUE}, weights are rescaled to sum to 1.}
}
\value{
The same object \code{x}, promoted to class \code{"MOProblem"}, with the method stored in
\code{x$method}.
}
\description{
Promotes a \code{prioriactions::Data} object to a multi-objective problem (class
\code{"MOProblem"}) and stores the configuration for the weighted-sum method.
}
\details{
This function assumes atomic objectives have already been registered using
\code{{prioriactions}} objective setters (e.g., \verb{add_objective_*()} with \verb{alias =}),
and that each objective alias is unique.
}
